ALGORITHM_FILE = "algorithms.txt"   -- The path to the algorithm file (leave blank if you want to enter algorithms as you go)
HISTORY_FILE_PATH = "history.txt"        -- The path to the history file (leave blank if you do not want to maintain statistics between sessions)
PRINT_CUBE = False                  -- (True or False) (Mostly for debugging) Whether or not the program should print out the 2d representation of the cube after the scramble is performed